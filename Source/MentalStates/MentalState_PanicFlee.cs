using System;
using Verse;
using RimWorld;

namespace RimWorld
{
    // Minimal stub to satisfy def type lookup at parse-time.
    // Intentionally does not inherit from the game's MentalState to avoid
    // requiring concrete base types during compile in the build environment.
    public class MentalState_PanicFlee
    {
    }
}
